import React, { Fragment } from "react";

const Position = ({ name }) => {
  return (
    <Fragment>
      <h1>{name}</h1>
      <h3>Position description goes here.</h3>
    </Fragment>
  );
};

export default Position;
